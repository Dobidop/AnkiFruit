[toolchain]
# Must match vendor/anki/rust-toolchain.toml — rslib's dependency graph pins a
# minimum rustc, and older toolchains fail to resolve it.
channel = "1.97.1"
targets = ["aarch64-apple-ios", "aarch64-apple-ios-sim"]
